#include <bits/stdc++.h>
using namespace std;

// 贪心专题 - 等价转换
class Solution {
public:
    bool closeStrings(string word1, string word2) {
        if (word1.size() != word2.size()) return false;
        
        for (int i=0;)
    }
};