#include <bits/stdc++.h>
using namespace std;

// 动态规划 - 11.1 前缀和优化 DP
class Solution {
public:
    double new21Game(int n, int k, int maxPts) {
        int begin = 0;
        double ans;

        
        auto dfs = [&](int i, double p) -> double {

        };

        return dfs(0, 1);
    }
};